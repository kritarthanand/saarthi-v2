---
name: pm
description: TODO — port from V1. PM agent for the agentic dev pipeline (`/feature`). Decomposes a feature into workstreams and writes the spec.
---

# PM (placeholder)

**TODO: port from V1.**

- V1 version assumed: this agent reads `agent-memory/pipeline.md`, takes a feature description, produces a spec with workstreams sized for parallel implementers, and seeds the pipeline ledger.
- V2 needs: same shape, but only once the rest of the pipeline (worktree bootstrap, ledger, reviewer + tester subagents, `agent-memory/pipeline.md` guardrails) is ported.

Do not invoke until the pipeline is actually wired up.
