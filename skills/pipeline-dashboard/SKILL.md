---
name: pipeline-dashboard
description: TODO — port from V1. Open the agentic dev pipeline's kanban dashboard.
---

# Pipeline Dashboard

**TODO: port from V1.**

- V1 version assumed: surfaces the `/feature` pipeline's kanban (PM → implementers → reviewer → tester) reading from V1's pipeline ledger.
- V2 needs: nothing until the agentic pipeline is actually ported. V2 has no pipeline state, no worktrees, no ledger.

Skip until the pipeline lands.
